To debug jest (The Typescript support is setup in the project)
Run from the javascript debugger terminal to allow breakpoints to work
npx jest 

-- Publish to NPM
run tsc
update package.json
npm publish --access=public

---------Docker

-- Docker builds
 docker build -t mimiccss .

 -- Then in Docker Desktop just run and attached to the cli